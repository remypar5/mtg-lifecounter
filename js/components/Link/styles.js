import { StyleSheet } from 'react-native';
import { COLOR_MARKED } from '../../utils/constants';

const styles = StyleSheet.create({
    link: {
        color: COLOR_MARKED,
    },
});

export default styles;
